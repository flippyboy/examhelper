services:
  examhelper:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./public/exams:/usr/share/nginx/html/exams:ro
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/"]
      interval: 30s
      timeout: 3s
      retries: 3
